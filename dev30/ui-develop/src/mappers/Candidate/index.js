export default class Candidate {
  constructor(id, fullName, email, cellPhone, lastActivity, status) {
    this.id = id;
    this.fullName = fullName;
    this.email = email;
    this.cellPhone = cellPhone;
    this.lastActivity = lastActivity;
    this.status = status;
  }
}
