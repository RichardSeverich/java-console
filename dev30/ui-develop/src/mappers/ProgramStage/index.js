/**
 * Mapper program stage used to manage UI responses and API resquet
 */
export default class ProgramStage {
  constructor(id, order, name, startDate) {
    this.id = id;
    this.order = order;
    this.name = name;
    this.startDate = startDate;
  }
}
